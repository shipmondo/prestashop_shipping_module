[tasks.precommit]
run = [
  "./build.sh",
  "npm install",
  "composer install",
  "npm run format",
  "./vendor/bin/php-cs-fixer fix",
]

[tasks.phpstan]
run = ["./vendor/bin/phpstan --memory-limit=-1"]
